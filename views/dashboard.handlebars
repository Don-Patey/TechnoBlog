<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Your Dashboard</title>
</head>

<body>
  <header>
    <h1>Your Dashboard</h1>
    <a href="/dashboard/add">Add New Blog Post</a>
  </header>

  <section>
    {{#if userPosts.length}}
      <h2>Your Blog Posts</h2>
      <ul>
        {{#each userPosts}}
          <li>
            <h3>{{this.title}}</h3>
            <p>{{this.body}}</p>
            <p>Created on {{this.createdAt}}</p>
            <a href="/post/{{this.id}}">Read more</a>
            <form action="/dashboard/{{this.id}}/delete" method="POST">
              <button type="submit">Delete</button>
            </form>
          </li>
        {{/each}}
      </ul>
    {{else}}
      <p>No blog posts yet. <a href="/dashboard/add">Create one!</a></p>
    {{/if}}
  </section>

  <footer>
    <p>&copy; 2024 Tech Blog</p>
  </footer>
</body>

</html>
